#include <iostream>
#include "bigint/bigint.h"
#include "bigint/functions.h"
using namespace std;
using namespace biginteger;

int main() {
    bigint a;
    /*
    for (bigint i = 2; i < 100; ++i) {
        //a = biginteger::get_prime(5);
        //cout << a << endl;
        cout << i << ' ' << is_prime(i, 50) << endl;
        //is_prime(i);
        //cout << get_prime(33) << endl;
    }
    */
    /*
    for (bigint i = 0; i < 100000; ++i) {
        a += i;
    }
    */
    //cout << is_prime(biginteger::random(12), 50, 12);
    //cout << is_prime(13, 50, 4);
    a = 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;
    a = a * 2;


    cout << a << endl;

    return 0;
}
//TODO: Check << ostream operator
