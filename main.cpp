#include <iostream>
#include "bigint/bigint.h"
#include "bigint/functions.h"

using namespace biginteger;

using namespace std;

int main() {
    /*
    for (int i = 2; i < 100; ++i) {
        cout << i << ' ' << is_prime(i, 3) << '\n';
    }
    */
    /*
    cout << bigint::zero << endl;
    cout << bigint::one << endl;
    cout << bigint::two << endl;
    cout << (0 == bigint::zero) << endl;
    cout << (1 == bigint::one) << endl;
    cout << (2 == bigint::two) << endl;
    */
    //bigint a = 112;
    bigint b = 12313512451;
    cout << b;
    return 0;
}
